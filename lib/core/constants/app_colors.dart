import 'package:flutter/material.dart';

class AppColors {
  // Uygulamanın açık, yumuşak arka plan rengi.
  static const Color background = Color(0xFFF1F5FA);

  // ana renk olarak kullanıyoruz.
  static const Color primary = Color(0xFF243A63);

  // Buton, ikon ve vurgularda kullanılacak mavi ton.
  static const Color accentBlue = Color(0xFF5A9DEE);

  // Öğün ekleme gibi sıcak aksiyonlarda kullanılacak turuncu/sarı ton.
  static const Color accentOrange = Color(0xFFF6B23C);

  // Kart arka planları için beyaza yakın ton.
  static const Color card = Color(0xFFFFFFFF);

  // Ana metin rengi.
  static const Color textDark = Color(0xFF263248);

  // İkincil açıklama metinleri.
  static const Color textMuted = Color(0xFF7E8AA3);

  // İnce çizgi ve input kenarlıkları.
  static const Color border = Color(0xFFD6DEEA);

  // Başarılı işlem mesajları için yeşil.
  static const Color success = Color(0xFF56B88A);

  // Uyarı ve silme aksiyonları için kırmızı.
  static const Color danger = Color(0xFFE56B6F);

  // Protein, karbonhidrat ve yağ noktaları için makro renkleri.
  static const Color protein = Color(0xFFFF8A78);
  static const Color carbohydrate = Color(0xFF5EB6E8);
  static const Color fat = Color(0xFFF3CF6B);
}
